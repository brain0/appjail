* change APPJAIL_SWAPDIR to a sane location, which can be changed in configure
* clean up unmount_directories and shared_directories handling, allow command line options for it
* add optional size limits for the tmpfs instances (SWAPDIR, /dev/shm)
* allow binding more file systems under /home
* (maybe) allow binding a custom {/var,}/tmp
* handle /run: keep, only keep /run/user/$UID, create private /run
* handle X11 sockets and .Xauthority?
* handle dbus sockets?
* add optional seccomp restrictions?
